import {View, Text} from 'react-native';
import React from 'react';

const SceneC = () => {
  return (
    <View
      style={{
        width: '100%',
        height: '100%',
        justifyContent: 'center',
        alignItems: 'center',
      }}>
      <Text>SceneC</Text>
    </View>
  );
};

export default SceneC;
