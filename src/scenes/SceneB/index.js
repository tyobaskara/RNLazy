import {View, Text} from 'react-native';
import React from 'react';

const SceneB = () => {
  return (
    <View
      style={{
        width: '100%',
        height: '100%',
        justifyContent: 'center',
        alignItems: 'center',
      }}>
      <Text>SceneB</Text>
    </View>
  );
};

export default SceneB;
